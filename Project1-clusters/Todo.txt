



A way to re-run clustering / refresh button



Add clustering state flag//A way to check if clustering has been computed

Add dynamic group count// algorhithm deciding groups or admin decides to set # of groups

Add better similarity metric (envelope, FFT, etc.)

Add animation when switching cluster modes
give colors to each groups






logging in
after login, and signout it does not remember the user
after guest log in and guest log out, the software crashes 